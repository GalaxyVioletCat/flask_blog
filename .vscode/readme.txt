Instruction to start on VSCode:
    1. Open flask_blog in terminal and enter "python -m venv blog_venv"
    2. turn to blog_venv virtualenviroment
    3. enter "pip install -r requirements.txt"
    4. debug by flask